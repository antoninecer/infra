[defaults]
inventory = hosts.ini
roles_path = roles
host_key_checking = False
# Disabling host key checking is not recommended for production environments.
# It's done here for simplicity in this new setup.
# In a real-world scenario, you should manage SSH keys properly.

[privilege_escalation]
become = true
